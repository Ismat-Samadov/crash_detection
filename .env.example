# No environment variables required for basic operation.
# The app is fully self-contained — data is generated server-side via /api/readings.

# Optional: override the polling interval (ms) — default 2000
# NEXT_PUBLIC_POLL_INTERVAL=2000
